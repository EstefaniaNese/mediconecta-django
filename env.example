# ====================================
# VARIABLES DE ENTORNO - MEDICONECTA
# ====================================
#
# IMPORTANTE: 
# 1. Copia este archivo como .env: cp env.example .env
# 2. Modifica los valores según tu entorno
# 3. NUNCA subas el archivo .env a Git (ya está en .gitignore)
#

# -------------------- Django --------------------
# SECRET_KEY: Clave secreta para firmar tokens, sesiones y cookies
#
# IMPORTANTE SOBRE SECRET_KEY:
# 1. Genera UNA CLAVE ÚNICA para ESTE entorno con el comando:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 2. Cada entorno (desarrollo, producción, otros desarrolladores) debe tener su PROPIA clave diferente
#
# 3. NUNCA uses la misma SECRET_KEY de producción en desarrollo
#
# 4. NUNCA cambies la SECRET_KEY de producción sin razón crítica porque:
#    - Invalida todas las sesiones activas (usuarios se desloguean)
#    - Invalida todos los tokens JWT existentes
#    - Solo cámbiala si fue expuesta/filtrada
#
# 5. Cambiar TU SECRET_KEY local NO afecta a producción (son independientes)
#
DJANGO_SECRET_KEY=cambia-esta-clave-por-una-generada-aleatoriamente

# Modo debug (1 para desarrollo, 0 para producción)
DJANGO_DEBUG=1

# Hosts permitidos (separados por coma)
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# CSRF Trusted Origins (solo para producción con HTTPS)
# En Railway, se configuran automáticamente basándose en ALLOWED_HOSTS
# Solo necesitas configurar esta variable si tienes dominios adicionales
# DJANGO_CSRF_TRUSTED_ORIGINS=https://tu-dominio.com,https://otro-dominio.com

# -------------------- Base de Datos --------------------
# Para desarrollo local, se usa SQLite automáticamente (no necesitas configurar nada)
# Para producción con PostgreSQL, descomenta y configura:
# DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_base_datos

# -------------------- Configuración Regional --------------------
# Zona horaria (ejemplo: America/Santiago, UTC, America/Mexico_City)
DJANGO_TIME_ZONE=UTC


